// components/Header.js

import React from "react";
import "../styles/Header.css"; // Import the stylesheet from the styles folder

const Header = () => {
  return (
    <div className="header-container">
      <h1 className="title">Task Manager</h1>
    </div>
  );
};

export default Header;
